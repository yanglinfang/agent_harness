# Source Modules

Core application modules will live here when the runtime grows beyond the
current `apps` and `packages` scaffolding.

Keep module-local `CLAUDE.md` files short and specific. They should explain
ownership, boundaries, and checks rather than repeating root-level instructions.
